# 303 - 本地编译 v0.1.189 并准备 GitHub 发布

## 基本信息

- 日期：2026-07-30
- 发布版本：`v0.1.189`
- 发布方式：Windows 安装包仅在本机编译，GitHub 只接收源码、标签和已验证产物
- 上一快照：`302-抽帧合并拼图取样与尾部覆盖完成.md`

## 任务目标

将抽帧尾部覆盖修复构建为正式版本并发布更新，同时确保 GitHub Actions 不参与编译。

## 已完成内容

### 1. 本地正式构建

使用项目正式构建脚本和 `windows-msvc-release-ffmpeg` 预设，在本机完成：

- Release 全量编译；
- Qt 运行时部署；
- FFmpeg / ffprobe 打包；
- 本地搜索运行时与模型无关载荷检查；
- Inno Setup 安装包生成。

正式产物：

`output/v0.1.189/CineVault-Setup-v0.1.189.exe`

### 2. 发布产物校验

- 文件大小：`210675204` 字节；
- ProductVersion：`0.1.189`；
- SHA-256：`87ec9e924390538ff48fbf481eca41aa8d381ed21723dd8b9aecbacc49c0745b`；
- 校验文件：`CineVault-Setup-v0.1.189.exe.sha256`；
- Authenticode：未签名，与当前项目既有本地发布策略一致。

### 3. 正式预设自动化测试

在 `windows-msvc-release-ffmpeg` 构建目录执行完整 CTest：

```text
49/49 passed
0 failed
```

其中包含新增的抽帧合并计划、真实 FFmpeg 抽取和界面契约回归。

### 4. 安装包真实冒烟测试

在系统临时目录完成：

1. 静默安装；
2. 检查既有模型文件不会被无模型安装包删除或覆盖；
3. 启动已安装的 `CineVault.exe`；
4. 静默卸载；
5. 清理临时安装目录。

结果：安装、启动、卸载全部通过。

## 关键发布约定

### 修改前

```text
GitHub 工作流监听 main 推送，普通提交可能触发云端 Windows 编译。
```

### 本次发布

```text
本机编译并验证安装包
→ 提交信息加入 [skip ci]
→ 推送 main
→ 确认该提交没有云端构建
→ 手动推送 v0.1.189 标签
→ 手动创建 Release 并上传本地产物
→ 对比 GitHub 资产摘要与本地 SHA-256
```

## 待办清单

- [ ] 提交并推送源码，确认 GitHub Actions 未执行编译；
- [ ] 创建并推送 `v0.1.189` 标签；
- [ ] 创建 GitHub Release，上传 `.exe` 和 `.sha256`；
- [ ] 核验 Latest Release、资产摘要和客户端更新契约；
- [ ] 清理发布过程中产生的临时目录并复查缓存体积。

## 验收标准

1. `main` 与 `v0.1.189` 指向同一提交；
2. GitHub Release 为正式 Latest Release；
3. Release 安装包文件名严格为 `CineVault-Setup-v0.1.189.exe`；
4. GitHub 资产摘要等于本地 SHA-256；
5. 对应源码提交没有执行 GitHub Actions 云端编译；
6. 本地工作区无未提交的任务修改。

## 下一步

按上述发布约定执行提交、标签、Release 上传和远端闭环验证。若发布中断，先读取本快照，再检查本地标签、远端标签和 Release 是否已存在，避免重复创建。
