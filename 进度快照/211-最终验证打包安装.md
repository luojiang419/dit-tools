# 211-最终验证打包安装

- 时间：2026-07-05 22:04:05
- 当前模块：模块 5，最终验证、安装包更新、安装版烟测

## 已完成内容

- 已读取最新快照 `210-多维度解析UI接入收口.md`，从模块 5 继续。
- 已创建阶段备份：`backup/v0.1.162`。
- 已确认下一构建产物版本为 `v0.1.133`。
- 已执行最终 Release/FFmpeg 构建验证。
- 已执行全量单元测试。
- 已生成新版安装包：
  - `output/v0.1.133/CineVault-Setup-v0.1.133.exe`
- 已计算安装包信息：
  - 大小：167,510,001 字节，约 159.75 MB
  - SHA256：`19257E5B829D20421EFBE4536C4ED0D3CAB7FCF526EBBA3780455B4F7A8FF011`
- 已使用新版安装包静默覆盖安装到本机已有安装目录：
  - `D:\Program Files\影资管家\CineVault.exe`
- 已确认安装日志成功：
  - `Installation process succeeded.`
  - `Need to restart Windows? No`
- 已启动安装版做 5 秒烟测：
  - 进程 5 秒后仍存活。
  - 烟测结束后已主动停止进程。
- 已清理临时目录：
  - `output/_installer_staging`
- 已生成阶段对比文档：
  - `backup/v0.1.162/最终验证打包-代码对比.md`

## 当前修改到哪个模块

- 本模块没有修改业务源码。
- 本模块更新构建产物、安装版状态与验证文档。

## 具体修改的代码前后对比

本模块无业务代码改动。

### 构建产物版本

修改前：

```text
最新安装包目录：output/v0.1.132
```

修改后：

```text
最新安装包目录：output/v0.1.133
安装包：output/v0.1.133/CineVault-Setup-v0.1.133.exe
```

### 最终构建配置

修改前：

```text
CINEVAULT_APP_VERSION:STRING=0.1.132
```

修改后：

```text
CINEVAULT_APP_VERSION:STRING=0.1.133
```

## 验证结果

- 最终构建：

```powershell
. "G:\data\app\DIT-tools\tool\windows_toolchain.ps1"
Invoke-VcVarsCommand "cmake --build --preset windows-msvc-release-ffmpeg --config Release --target CineVault"
```

结果：通过，`ninja: no work to do.`

- 全量测试：

```powershell
ctest --test-dir build/windows-msvc-release-ffmpeg --output-on-failure
```

结果：13/13 通过。

- 安装包构建：

```powershell
powershell -ExecutionPolicy Bypass -File tool\build_windows.ps1 -EnableFfmpeg
```

结果：通过，生成 `output/v0.1.133/CineVault-Setup-v0.1.133.exe`。

- 静默安装：

```powershell
CineVault-Setup-v0.1.133.exe /VERYSILENT /SUPPRESSMSGBOXES /NORESTART
```

结果：退出码 0。

- 安装版烟测：

```powershell
D:\Program Files\影资管家\CineVault.exe
```

结果：启动后 5 秒仍存活，随后已停止烟测进程。

## 待办清单（未完成）

- 人工进入安装版界面，使用真实素材验证：
  - 多维度解析按钮状态。
  - 多维度解析弹窗打开和自定义维度添加。
  - 模型返回后结果卡片刷新展示。
- 如需发布，再执行 GitHub 发布流程。

## 下一步要做什么

- 等待人工验收多维度解析真实模型链路；若验收通过，可进入发布流程。

