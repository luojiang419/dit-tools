# 298-v0.1.186版本发布规则与本地安装包完成

## 已完成内容

本模块处理发布链路版本冲突并完成本地正式打包。

当前版本状态：

- GitHub Latest：`v0.1.180`
- 本机已安装：`v0.1.185`
- 仓库 `VERSION`：`0.1.186`

已采用策略：发布源码显式版本 `v0.1.186`。原因是若按远端 Latest 递增发布 `v0.1.181`，会低于用户已安装版本，客户端不会检测到更新。

## 当前修改到哪个模块

当前模块：发布版本解析与本地打包验收。

相关文件：

- `tool/resolve_release_version.ps1`
- `tool/test_release_version.ps1`
- `backup/v0.1.181/显式源码版本发布规则修复-代码前后对比.md`
- `避坑指南/远端Latest落后本地安装版本时不能按下一补丁低版本发布.md`

## 代码前后对比

### 修复前

```powershell
if ($sourceVersionTag -cne $versionTag) {
    throw "Repository VERSION $sourceVersionTag must equal latest release $normalizedLatestVersion or the next release $versionTag."
}
```

`VERSION=0.1.186`、Latest=`v0.1.180` 会被拒绝。

### 修复后

```powershell
if ($comparisonWithLatest -lt 0) {
    throw "Repository VERSION $sourceVersionTag is lower than latest release $normalizedLatestVersion."
}

$comparisonWithNext = Compare-CineVaultVersions -Left $sourceVersionTag -Right $nextVersionTag
if ($comparisonWithNext -gt 0) {
    Write-Host "Repository VERSION $sourceVersionTag is higher than the next patch $nextVersionTag; publishing the explicit source version."
}
```

源码版本高于 Latest 时发布源码显式版本；低于 Latest 时拒绝。

## 验证

- `tool/test_release_version.ps1`：通过
- `tool/resolve_release_version.ps1 -LatestVersion v0.1.180` 输出：
  - `should_publish=true`
  - `version_bump_required=false`
  - `version_tag=v0.1.186`
  - `app_version=0.1.186`
- 本地正式打包：通过
- 安装包校验：通过
- 签名状态：`NotSigned`

## 本地产物

```text
G:\data\app\DIT-tools\output\v0.1.186\CineVault-Setup-v0.1.186.exe
```

校验：

```text
Size:    210699786 bytes
SHA256:  d48f5847d9375ca9ac0bebbd26adaba4039f1e4670180b7783cc74afce3adfd3
```

## 重要说明

本地未运行 `tool/test_installer_startup.ps1`，因为它会写临时卸载注册表，脚本标注只应在 disposable CI runner 上使用。当前本地已由 `tool/build_windows.ps1` 完成 staging 启动探针；正式发布还需要 GitHub Actions 在干净 runner 上完成远端复核。

## 待办清单

- 提交所有本次修复和文档。
- 推送 `main`。
- 使用代理 `http://127.0.0.1:7890` 监控 GitHub Actions。
- 确认 Actions 成功、Release 非草稿、tag/branch/release SHA 一致。
- 校验 Release 资产和 `.sha256` 可下载且内容一致。
- 清理本轮临时探针日志和临时输出。

## 下一步

进入提交、推送和远端发布监控。
