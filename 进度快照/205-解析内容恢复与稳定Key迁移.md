# 205-解析内容恢复与稳定Key迁移

- 时间：2026-07-05
- 当前模块：素材管理中心解析结果恢复与同步防回归

## 已完成内容

- 已读取上一份最新快照：`进度快照/204-全局索引空字段修复.md`。
- 已创建阶段源码备份：`backup/v0.1.158/`。
- 已备份真实数据库：`backup/v0.1.158/runtime-db-before-restore/`。
- 已补充本模块代码和数据恢复对比文档：`backup/v0.1.158/解析内容恢复与稳定Key迁移-代码对比.md`。
- 已按用户授权结束残留 `CineVault.exe` 进程，避免恢复期间应用同时写库。
- 已确认“解析内容消失”不是彻底删除，而是旧解析结果仍在旧 `video_key`，当前素材重扫后换了新 `asset_id`，导致界面按新 key 查不到旧解析。
- 已 dry-run 映射旧解析到当前素材：
  - 145 条按稳定偏移映射。
  - 3 条按 `source_root=5` 的当前视频顺序映射。
  - 目标汇总冲突为 0。
  - 目标帧冲突 37 条、目标任务冲突 1 条，属于重新解析失败留下的半成品，迁移时已清理。
- 已执行真实数据库恢复：
  - 汇总解析迁移：148 条。
  - 帧解析迁移：14075 条。
  - 任务记录迁移：126 条。
  - 当前素材可关联汇总解析：148 条。
  - orphan 汇总解析：0 条。
  - Ready 素材：148 条。
- 已修改同步逻辑：同一路径素材只因 `asset_id` 变化导致 key 改变时，自动迁移解析结果、帧结果、任务和 FTS 索引到新 key。
- 已修改同步逻辑：未变化素材不再用空元数据 FTS 覆盖已有解析搜索文本。
- 已新增回归测试覆盖同路径换 key 的解析迁移。
- 已重新构建 Release + FFmpeg 安装包。
- 已清理构建后的空临时目录 `output/_installer_staging`。

## 当前修改到哪个模块

- `dit-tools-src/cinevault-pro/src/application/MaterialCatalogSyncService.cpp`
- `dit-tools-src/cinevault-pro/tests/unit/MaterialCatalogSyncServiceTest.cpp`

## 具体修改的代码前后对比

详细对比见：`backup/v0.1.158/解析内容恢复与稳定Key迁移-代码对比.md`。

### 同路径旧 key 迁移

新增：

```cpp
bool migrateAnalysisArtifacts(QSqlDatabase &globalDb,
                              const QString &oldVideoKey,
                              const QString &newVideoKey,
                              bool hasFts5,
                              QString *errorMessage)
```

### FTS 覆盖保护

修改前：

```cpp
if (!upsertSearchRow(globalDb, asset, hasFts5, errorMessage)) {
    globalDb.rollback();
    return false;
}
```

修改后：

```cpp
if (remappedExisting && !changed
    && !migrateAnalysisArtifacts(globalDb, existing.videoKey, asset.videoKey, hasFts5, errorMessage)) {
    globalDb.rollback();
    return false;
}

if (changed && !upsertSearchRow(globalDb, asset, hasFts5, errorMessage)) {
    globalDb.rollback();
    return false;
}
```

## 验证结果

- `cmake --build --preset windows-msvc-release-ffmpeg --config Release --target MaterialCatalogSyncServiceTest`：通过。
- `ctest --test-dir build\windows-msvc-release-ffmpeg --output-on-failure -C Release -R MaterialCatalogSyncServiceTest`：通过。
- `ctest --test-dir build\windows-msvc-release-ffmpeg --output-on-failure -C Release`：12/12 通过。
- `powershell -ExecutionPolicy Bypass -File .\tool\build_windows.ps1 -Configuration Release -EnableFfmpeg`：通过。
- `git diff --check`：通过，仅有 LF/CRLF 换行策略提示。
- 最新安装包：`G:\data\app\DIT-tools\output\v0.1.130\CineVault-Setup-v0.1.130.exe`。

## 待办清单（未完成）

- 建议安装 `output/v0.1.130/CineVault-Setup-v0.1.130.exe`，让防回归逻辑进入当前安装版本。
- 打开项目 `D:/data/影视管家/002/project.cvdb`，进入素材管理中心，抽查已解析视频详情，应能看到恢复后的摘要和帧分析。
- 若继续解析时报 `response_format.type` 400，需要单独修复视觉接口请求参数；这与本次恢复不同，是接口兼容问题。

## 下一步要做什么

- 安装 v0.1.130 并进行人工验收；重点看已解析视频详情页、搜索命中和新解析任务是否正常。
