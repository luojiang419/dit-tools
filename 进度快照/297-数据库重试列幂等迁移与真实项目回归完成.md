# 297-数据库重试列幂等迁移与真实项目回归完成

## 已完成内容

本模块解决真实项目回归中新增暴露的问题：

- 真实项目 `D:\data\影视管家\服装\project.cvdb` 的 `schema_version=9`，但 `media_metadata` 缺少 retry 列。
- 旧代码在补列前创建 `idx_media_metadata_retry`，导致 `no such column: next_retry_at`，迁移事务回滚。
- 已将 `media_metadata / thumbnail / embedded_metadata` 的 retry 索引创建移动到补列之后。
- 新增迁移回归测试，覆盖 `schema_version=9` 和 `schema_version=CurrentSchemaVersion` 但表列缺失的自愈场景。

## 当前修改到哪个模块

当前模块：项目数据库 schema 幂等自愈。

相关文件：

- `dit-tools-src/cinevault-pro/src/infrastructure/db/DatabaseManager.cpp`
- `dit-tools-src/cinevault-pro/tests/unit/FolderDatabaseMigrationTest.cpp`

## 代码前后对比

### 修复前

```cpp
QStringLiteral("CREATE INDEX IF NOT EXISTS idx_media_metadata_retry "
               "ON media_metadata(probe_status, next_retry_at, retry_policy_version, asset_id);")

if (!executeBatch(db, statements, errorMessage)) {
    return false;
}

return ensureColumns(db, QStringLiteral("media_metadata"), {...}, errorMessage);
```

旧表没有 `next_retry_at` 时，索引创建先失败，补列逻辑不会运行。

### 修复后

```cpp
if (!executeBatch(db, statements, errorMessage)) {
    return false;
}

if (!ensureColumns(db, QStringLiteral("media_metadata"), {...}, errorMessage)
    || !ensureColumns(db, QStringLiteral("media_stream"), {...}, errorMessage)
    || !ensureColumns(db, QStringLiteral("thumbnail"), {...}, errorMessage)) {
    return false;
}

return executeBatch(db,
                    {QStringLiteral("CREATE INDEX IF NOT EXISTS idx_media_metadata_retry "
                                    "ON media_metadata(probe_status, next_retry_at, retry_policy_version, asset_id);"),
                     QStringLiteral("CREATE INDEX IF NOT EXISTS idx_thumbnail_retry_schedule "
                                    "ON thumbnail(status, next_retry_at, retry_policy_version, asset_id);")},
                    errorMessage);
```

`embedded_metadata` 同步采用“补列后建 retry 索引”的顺序。

## 验证

- `FolderDatabaseMigrationTest`：通过
- 全量构建：通过
- 全量 CTest：47/47 通过，总耗时约 40 秒
- `git diff --check`：通过，仅 CRLF 提示
- QML lint：33 文件，725 diagnostic，0 error，未超过 731 基线
- 真实项目回归：
  - 打开前：`schema_version=9`，`media_metadata` 只有旧列，0 行
  - 打开后：`schema_version=10`，retry 列补齐，`media_metadata` 写入 79 行
  - 元数据任务已从截图中的 0/128 推进到 79/128

## 新发现的外部阻断

视觉分析真实探针已越过数据库问题，但 LM Studio 返回：

```text
401 Invalid LM Studio API token
```

当前全局分析任务状态：98 帧已处理，87 成功，11 失败；失败原因均为 401。多数失败行仍保留已有视觉内容，只是状态和错误被标记。需要用户更新 LM Studio/API token 后再做完整真实视觉端到端成功验收。

## 待办清单

- 确认本次发布版本策略：正式发布 `v0.1.186`，还是按远端 Latest 从 `v0.1.181` 起补发。
- 若选择 `v0.1.186`：补强 `resolve_release_version.ps1`，允许源码显式版本高于远端 Latest。
- 执行本地正式打包、安装包校验、签名状态记录。
- 提交、推送并跟踪 GitHub Actions 到 Release 正式发布成功。
- 清理本次临时探针日志和测试输出，保留必要诊断记录。

## 下一步

进入版本发布链路：先处理远端 Latest `v0.1.180` 与本地 `VERSION=0.1.186` 的版本策略冲突，然后打包、推送、监控 CI 和 GitHub Release。
