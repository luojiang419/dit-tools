# 292 - 打包 v0.1.184 安装包完成

## 本轮目标

- 使用当前工作区源码生成 Windows Release 安装包 `v0.1.184`。
- 不发布 GitHub Release，不提交或覆盖现有未提交修改。
- 校验安装包版本、完整性、启动与卸载清理。

## 已完成内容

1. 确认仓库根目录 `VERSION` 为 `0.1.184`，未再次递增版本号。
2. 确认 Windows 打包工具链可用：
   - Qt `6.6.3`（MSVC 64 位）
   - MSVC Release 真实工作流
   - FFmpeg 开发包与 CLI 运行时
   - Inno Setup 编译器
3. 执行：

   ```powershell
   .\tool\build_windows.ps1 -Configuration Release -Version v0.1.184 -RealWorkflow
   ```

4. Inno Setup 编译成功，生成安装包和 SHA-256 校验文件。
5. 执行制品校验：文件名、ProductVersion、最小体积、SHA-256 均通过。
6. 执行发布版本规则测试，通过。
7. 在系统临时目录执行静默安装、QML 启动探针和卸载，全部通过；临时安装目录及相关进程无残留。
8. 打包中间部署目录已由脚本自动清理；保留 `1.81 GiB` 可复用构建缓存，未超过 20 GiB 限制。

## 最终制品

- 安装包：`G:\data\app\DIT-tools\output\v0.1.184\CineVault-Setup-v0.1.184.exe`
- 校验文件：`G:\data\app\DIT-tools\output\v0.1.184\CineVault-Setup-v0.1.184.exe.sha256`
- ProductVersion：`0.1.184`
- 文件大小：`210688875` 字节（约 `200.93 MiB`）
- SHA-256：`9a5e9e548ca0ca8d2ba094392f0e4eb0bf20da65b26dcdf1be9674f4bab35e67`

## 本轮代码前后对比

本轮只执行打包、校验和临时环境清理，没有修改产品源代码。

```text
修改前：v0.1.184 源码与完整测试均已完成，但尚无本版本安装包。
修改后：源码保持不变，新增 v0.1.184 安装包及其 SHA-256 校验文件。
```

## 验收结果

- [x] 目标版本严格为 `v0.1.184`
- [x] Release 真实工作流构建成功
- [x] Inno Setup 安装包生成成功
- [x] 安装包 ProductVersion 校验通过
- [x] SHA-256 校验文件生成成功
- [x] 发布版本规则测试通过
- [x] 临时静默安装成功
- [x] 安装后 QML 启动探针通过
- [x] 临时卸载及残留清理完成
- [x] 构建缓存低于 20 GiB

## 当前模块

- `v0.1.184` Windows 本地打包与验收：已完成。

## 待办清单

- 无本地打包待办。
- 如需对外发布，后续单独执行代码签名、GitHub Release 上传与在线更新链路验证；本轮未获得发布指令，因此未执行。

## 下一步

- 可直接交付 `output\v0.1.184` 下的安装包与校验文件。
- 若用户明确要求发布，再进入签名与 GitHub Release 流程。
