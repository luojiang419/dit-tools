# 223-发布v0.1.142到GitHub

## 已完成内容

- 已读取最新快照：`进度快照/222-视觉接口JSON结构修复.md`。
- 已读取发布规范：`docs/GitHub发布更新说明.md`。
- 已确认应用内更新读取 GitHub `latest release`：

```text
https://api.github.com/repos/luojiang419/dit-tools/releases/latest
```

- 已确认发布目标安装包存在：

```text
output/v0.1.142/CineVault-Setup-v0.1.142.exe
大小：167524545 bytes
SHA256：86EE765CE7A4AAB58FB31A188F18F2B562C85E2741B094A1CF6079DAE39EC6DC
```

- 已创建本阶段备份：`backup/v0.1.171`。
- 已创建公开 GitHub Release：

```text
https://github.com/luojiang419/dit-tools/releases/tag/v0.1.142
```

- 已上传安装包资产：

```text
名称：CineVault-Setup-v0.1.142.exe
下载：https://github.com/luojiang419/dit-tools/releases/download/v0.1.142/CineVault-Setup-v0.1.142.exe
GitHub digest：sha256:86ee765ce7a4aab58fb31a188f18f2b562c85e2741b094a1cf6079dae39ec6dc
```

- 已验证 GitHub `latest release` 已指向 `v0.1.142`。

## 当前修改到哪个模块

- 当前模块：GitHub 发布模块。
- 本阶段没有业务源码修改。
- 本阶段新增本地记录文件：
  - `backup/v0.1.171/本轮修改溯源.md`
  - `进度快照/223-发布v0.1.142到GitHub.md`

## 具体修改的代码前后对比

本阶段没有业务代码变更，只有 GitHub 远端发布状态变化。

发布前：

```text
GitHub latest release：v0.1.126
v0.1.142 release：不存在
v0.1.142 安装包资产：不存在
```

发布后：

```text
GitHub latest release：v0.1.142
v0.1.142 release：已公开
v0.1.142 安装包资产：已上传
```

Release 创建命令摘要：

```powershell
gh release create v0.1.142 ".\output\v0.1.142\CineVault-Setup-v0.1.142.exe" `
  --repo luojiang419/dit-tools `
  --target main `
  --title "CineVault v0.1.142" `
  --latest
```

## 验证记录

- `gh auth status`：账号 `luojiang419` 已登录。
- `gh release view v0.1.142 --repo luojiang419/dit-tools`：Release 存在。
- `gh api repos/luojiang419/dit-tools/releases/latest`：返回 `tag_name=v0.1.142`。
- `gh release list --repo luojiang419/dit-tools --limit 5`：`CineVault v0.1.142` 显示为 `Latest`。
- `git ls-remote --tags origin refs/tags/v0.1.142`：远端 tag 存在。

## 待办清单（未完成）

- 用旧版本客户端人工检查更新，确认能检测到 `v0.1.142` 并下载 `CineVault-Setup-v0.1.142.exe`。
- 当前本地工作树有大量未提交源码改动；本次为“用户更新优先”的安装包发布，没有把这些源码改动强行提交到 `main`。
- 如需要源码 tag 与安装包内容完全一致，后续应单独整理、提交、推送当前源码，再发布新的源码一致版本。
- 继续单独排查 `ImportServiceLegacyRescanTest` SegFault。

## 下一步要做什么

- 使用旧版安装客户端触发一次应用内更新检查。
- 如果客户端能看到 `v0.1.142` 并下载成功，本次用户更新发布闭环完成。
- 若客户端仍看不到更新，下一步读取自动更新日志，重点检查 GitHub latest API、资产命名匹配和网络代理。

