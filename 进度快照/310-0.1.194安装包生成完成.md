# 进度快照 310

- 时间：2026-07-31
- 当前版本：`0.1.194`
- 本次目标：把已完成的内置 Web 搜索服务生命周期代码制作成可部署安装包，供软件启动后直接提供浏览器访问地址。
- 当前状态：安装包已生成并通过发布物校验；尚未替换用户当前运行的旧安装版。

## 本次完成

### 1. 正式 Windows 安装包

使用真实工作流执行：

```text
powershell -ExecutionPolicy Bypass -File tool/build_windows.ps1 -RealWorkflow -Version v0.1.194
```

生成文件：

```text
output/v0.1.194/CineVault-Setup-v0.1.194.exe
```

发布物信息：

- 产品版本：`0.1.194`
- 文件大小：`210683848` bytes
- SHA-256：`7b23b03188f5c17a254c2a9e62529a47976d85c80eed5442d4d0117c99a29547`
- 校验文件：`output/v0.1.194/CineVault-Setup-v0.1.194.exe.sha256`

### 2. 打包链路验证

- Qt 运行时部署：通过。
- QML 启动烟测：通过。
- 安装包产品版本校验：通过。
- 安装包大小下限和 SHA-256 生成：通过。
- Web 页面资源继续由 Qt Resource 编译进程序，不依赖安装目录中的额外 Web 文件。

## 当前未执行事项

当前运行进程仍为：

```text
D:\Program Files\影资管家\CineVault.exe
```

该进程属于旧安装版，且没有监听 `17890`。本次没有强制结束它，也没有静默安装新包，避免中断用户当前工作。

## 待办清单

1. 用户关闭旧版软件。
2. 运行 `CineVault-Setup-v0.1.194.exe` 完成覆盖安装。
3. 启动新版软件后访问 `http://127.0.0.1:17890/`。
4. 访问 `http://127.0.0.1:17890/api/health`，确认返回 `ok=true`。
5. 关闭新版软件，确认 TCP `17890` 不再处于 LISTEN 状态。
6. 其他设备访问时，如失败，再单独放行 Windows 防火墙 TCP `17890` 入站规则。

## 下一步

安装 0.1.194 后做一次真实运行回归；若用户只需要本机浏览器访问，优先使用 `127.0.0.1` 地址，避免额外暴露局域网端口。
