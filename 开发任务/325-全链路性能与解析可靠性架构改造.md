# 全链路性能与解析可靠性架构改造

状态：代码改造及局部验证完成，正在进行发行构建与百万文件验收
当前阶段：6/6
最后更新：2026-09-07（Asia/Shanghai）

## 当前状态

- 分支：`feat/architecture-reliability-20260905`；本轮起点 `d4bd1e3`。
- 已完成监听、抽帧时间覆盖、重建检查点、详情请求背压、任务落盘与索引并发隔离的代码改造。
- 48 项非压力 CTest 全部通过；最后的检查点恢复、Job写锁预算、抽帧覆盖记录改动已补跑相关测试并通过。
- 已生成真实的一百万个空 JPG 文件（1000个目录）；真实扫描协调器与精确数量断言的压力测试正在运行。空文件只验证枚举/入库，不代表一百万个媒体文件解码。
- 准备以全依赖 `windows-msvc-release-real` 预设在新目录 `build/release-v0_1_204` 构建。旧 `windows-msvc-release-real` 缓存指向 `G:/data/app/DIT-tools` 且MSVC依赖前缀乱码，不能复用。
- 保留用户原有 `.gitignore`、`dependencies.lock` 删除状态及历史未跟踪文件；没有无差别暂存。

## 下一步

1. 完成百万目录测试，读取 `output/reliability-20260907/million-catalog.json` 和测试日志。
2. `tool/build_windows.ps1 -RealWorkflow -Version v0.1.204 -BuildDirectoryName release-v0_1_204`，完整依赖构建安装包。
3. 在发行目录运行完整 CTest、QML 门禁和正式 staging 启动探针，验证包名、版本、SHA-256、无Qwen模型载荷。
4. 更新本文，精确提交本轮文件并正常 push。交付 `output/v0.1.204/CineVault-Setup-v0.1.204.exe`。

## 当前 TODO

- [x] 模块1：盘符根路径、整盘监听与公共路径契约。
- [x] 模块2：真实PTS、首尾锚点、VFR/非零起点、PerFrame全帧保留、覆盖记录及2万候选帧保护。
- [x] 模块3：失败保留旧解析、新代逐帧检查点、恢复复用、完整后发布、提交前取消检查。
- [x] 模块4：异步只读查询、每类一运行加一最新待办、24帧分页、240帧/8MiB滚动窗口、快捷搜索工程导航。
- [x] 模块5：扫描统计索引、writer租约、后台Job合并落盘、生产者退出后排空、低频同步创建/删除50ms锁预算。
- [x] 模块6：跨实例/进程QLockFile写入所有权、单次bulk发布、失败按当前目录全量校正、启动时从持久目录恢复索引。
- [x] 模块7：Web查询边界、Vision HTTP取消、FFmpeg取消。
- [ ] 百万目录压力测试结果核对。
- [ ] 全依赖构建、完整回归与QML启动验收。
- [ ] 安装包校验与交付。
- [ ] 最终文档、Git提交及推送。

## 最近验证状态

- 已验证目录：`dit-tools-src/cinevault-pro/build/architecture-verified-utf8`，Release、local-search ON，RAW/ExifTool/assistant OFF。
- 完整常规 CTest：48/48通过，51.44秒；未将无fixture的压力入口计入。
- `ReliabilityRecoveryTest`：10万次请求只保留最新待办；耗尽重试/结构化不完整帧每轮仅调度一次；检查点跨实例恢复、64位PTS/文件大小、模型/源指纹变化拒绝复用、损坏检查点报错、原图保留。
- `SemanticSearchIndexServiceTest`：互斥写者、陈旧实例重载、未发布bulk恢复与旧有用例通过。
- `FFmpegAdapterFrameExtractionTest`：首尾、短片、取消、三种策略VFR/非零起点及2小时合成视频通过；所有返回时间都属于真实源帧，2小时视频按240秒间隔输出31条覆盖记录。
- `JobEngineTest`：写锁下进度非阻塞、同步创建快速失败、协调器关闭后最终排空通过。
- 本轮新增功能编入 `CineVault` 主目标成功。
- 正式依赖包和运行环境验证尚待完成，不能用上述精简构建替代。

## 任务目标

修复审计中已确认的监听、时间覆盖、解析恢复、前台阻塞、扫描统计和索引发布问题，使大目录、长视频与搜索共同遵守有界资源、可恢复发布和可验证覆盖。

## 技术方案与当前关键修改

- `FFmpegAdapter`：所有策略用源帧选择和 `-copyts`，移除fps生成网格与缺PTS时的估算；PerFrame和间隔锚点不被质量/去重筛掉。覆盖记录 `coverage.json` 保存源首尾、候选PTS、是否必保和保留/筛除原因。超过20000候选明确失败并提示分段或增大间隔。
- `AnalysisRunStore`：`pending-run.json` 为新代清单，每帧独立 `analysis-N.json` 用QSaveFile提交。视频/采样/源文件/模型匹配才恢复；中断保留已付费结果。旧代保持可读，新代全部结构化完成并汇总成功后事务替换。
- `FrameRequestSchedule`：每个候选每轮最多进入一次批次；重试由单请求内3次上限控制，并发最多8。
- `LatestRequestQueue`、`MaterialCenterViewModel`：搜索、详情和拼图分别最多一运行、一最新待办。详情窗口移出旧页后继续前进，避免240帧后无法浏览。快捷搜索直接使用命中结果的工程信息。
- `SemanticSearchIndexService`：文件锁覆盖整个批次的begin/apply/publish生命周期，新写者重新加载最新索引；查询仍使用现有只读/关键词回退路径。
- `SearchDocumentSyncService`：任何同步失败改为按当前目录校正，避免旧Removed/Updated覆盖新事件。AppContext已在每次启动安排全量校正，因此持久目录与search_document构成恢复源，不再另建重复outbox。移除覆盖整段embedding的项目SQLite writer租约。
- `JobEngine` / `AppContext`：停止所有生产者后最后排空；此时绕过已关闭协调器。低频创建/删除保留现有同步成功契约，但锁等待限制50ms；高频进度仍后台写入。清理终态任务按内存终态ID删除，避免终态尚未落盘时漏删。
- `ScanEngine` / `ScanPathPolicy`：暂存统计在恢复时校准、批次提交后累加、目录重扫后扣减；整卷标志每轮仅计算一次，取消逐文件磁盘卷刷新。
- `build_windows.ps1`：增加可选独立构建目录，沿用现有正式依赖和打包流程。

## 验收边界与已知限制

- 百万测试中发现并修复了进度反复全表汇总和逐文件刷新磁盘卷信息；测试已保留中断数据库并在新代码上恢复，结果待最终核对。
- 百万空文件不覆盖真实媒体解码、损坏RAW或真实网络盘断连。用户故障视频、卡死转储及网络盘环境未提供。
- 解析检查点复用以已原子保存的单帧结果为界；进程在收到响应但保存之前被强杀，仍可能需要重请求该帧。
- 未发布/历史解析图片为保证旧代和恢复安全保留；本轮不自动删除无法确认引用的旧运行目录。
- 低频Job创建/删除是有界同步接口；没有改变为异步成功通知这一公共API。
- 真正的系统安装会修改现有安装注册；本次用户要求编译安装包，使用正式部署载荷启动验收，不自动覆盖安装到用户当前环境。

## 文件 / 模块清单

主代码：AppContext、VideoAnalysisService、AnalysisRunStore、FrameRequestSchedule、LatestRequestQueue、MaterialCenterViewModel、FFmpegAdapter、VisualAnalysisMetadata、JobEngine、SearchDocumentSyncService、SemanticSearchIndexService。
测试：ReliabilityRecoveryTest、FFmpegAdapterFrameExtractionTest、SemanticSearchIndexServiceTest、JobEngineTest、LargeCatalogStressTest。
构建/文档：CMakeLists、build_windows.ps1、VERSION、本文及必要避坑文档。

## 开发阶段

- [x] 阶段1：恢复现场、建立单一任务文档与分支
- [x] 阶段2：时间覆盖和重建正确性
- [x] 阶段3：前台与数据库执行边界
- [x] 阶段4：任务和索引可靠性
- [ ] 阶段5：综合压力回归
- [ ] 阶段6：发行构建与交付

## 开发日志

- 2026-09-05：从审计结果进入实施，创建功能分支和单一任务文档。
- 2026-09-05：完成模块1，干净构建目录编译并通过7项后台监控测试。旧构建缓存路径失效，没有运行旧二进制。
- 2026-09-05：完成模块2/3；全量测试首次发现旧实体/OCR断言与22字段故事板迁移冲突，修正请求 Schema 和测试后46/46通过。
- 2026-09-05：完成模块4；相关查询/UI测试46项合计通过，完整应用增量构建和QML启动探针通过。
- 2026-09-05：完成模块5；曾因旧增量对象未随JobEngine布局变化重编译发生ABI崩溃，执行构建目标clean后当前源码测试通过。
- 2026-09-05：完成模块6；启用真实本地语义依赖验证单次索引发布和失败自动重放，完整应用构建通过。
- 2026-09-05 19:23：按用户要求暂停。复核发现之前完成标记过早，已重新打开模块2–6并记录具体缺口；最新模块7Web测试失败。扫描/DB/工具链纠错及模块7代码保留未提交，不再继续开发。
- 2026-09-05 20:45：工具链切换 UTF-8 代码页后，新构建目录的 MSVC 依赖前缀正确；Web 超限请求4/4通过；真实协调器扫描、根目录计数和数据库迁移测试通过。下一步转向抽帧覆盖、Job writer、UI背压和搜索恢复。
- 2026-09-05 21:25：抽帧尾部改为固定尾窗解码到 EOF，使用 `-copyts` 保留真实源 PTS；`FFmpegAdapterFrameExtractionTest` 5/5通过。
- 2026-09-05 21:35：Job writer 排空改为显式消费 future 结果并重放失败批次；`JobEngineTest`通过。
- 2026-09-05 22:05：JobEngine writer 接入 `IndexingWorkCoordinator`，修复定向扫描期间 SQLite writer 竞争；JobEngine/扫描回归2/2通过。
- 2026-09-05 23:05：默认全目标构建完成，完整 CTest 48/48通过；任务仍保留重建恢复、搜索并发快照、真实压力 fixture 等后续缺口。
- 2026-09-05 21:50：素材中心详情分页增加 240 帧硬上限与背压提示；`CineVault` 主目标完整编译通过。
- 2026-09-06：VisionApiClient 增加 stop_token HTTP 中止与重试等待取消；MaterialCenter 详情分页增加 8 MiB 字节预算；主目标及 Vision/MaterialCenter 回归通过。


- 2026-09-07：接力复核并完成上述剩余代码改造。48项常规回归通过，新增真实百万文件fixture及发行构建目录支持，进入最终打包验收。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

阅读项目规则和本文，检查 branch/status/log；从“下一步”继续。旧业务已提交，用户的两个既有删除与历史输出保持原状。以实际源代码、Git和新测试日志为准，不重复旧审计，不把待验证项提前标为完成。
