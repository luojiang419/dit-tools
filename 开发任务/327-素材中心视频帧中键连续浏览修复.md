# 素材中心视频帧中键连续浏览修复

状态：已完成，v0.1.206安装包已验证
当前阶段：3/3（交付完成）
最后更新：2026-09-07

## 当前状态

- 分支：`feat/architecture-reliability-20260905`，起点 `819e401`。
- 已确认根因：外层详情 ScrollView 安装中键处理器，内层固定高度 PagedFrameList 缺失处理器，中键滚动外层到底后停止。
- 已在 PagedFrameList 安装既有 MiddleDragScrollHandler，公共组件及数据接口无改动。
- 安装包：`output/v0.1.206/CineVault-Setup-v0.1.206.exe`，包含FFmpeg工具，无需重解析原有素材。
- 保留原有 `.gitignore`、`dependencies.lock` 删除及未跟踪文件，提交仅包含本任务文件。

## 下一步

当前任务已完成。安装v0.1.206后，在右侧帧卡区域按住中键并向下偏移，即可连续跨页浏览。最终代码提交与远端状态见当前分支Git记录；没有安装覆盖用户当前程序。

## 当前 TODO

- [x] 嵌套列表运行时复现。
- [x] 最小修复与行为回归。
- [x] 构建与安装包交付验证。

## 最近验证状态

- 修复前：正确携带 Qt.MiddleButton 的鼠标移动事件复现内层不能滚动，回归测试失败（`output/middle-scroll-before.txt`）。
- 修复后：Qt Quick 9 passed / 0 failed / 0 skipped，12.786秒（`output/middle-scroll-after.txt`）。
- 一次中键按住、指针移动后静止，自动滚动到第505帧并跨240条缓存窗口；外层不漂移；松开停止、反向滚动、外层滚动、滚轮分页及失败重试通过。
- 在 offscreen 环境使用 Basic Controls 样式，避免原生 Windows 主题接口依赖；测试输入没有操作用户正在使用的窗口。
- 相关CTest 3/3通过（PagedFrameListTest、MaterialCenterQueryServiceTest、MaterialCenterUiContractTest），13.26秒。
- QML门禁：34文件、724条既有诊断、0 error，未增加诊断或放宽门禁。`git diff --check`通过。
- Release全目标构建及部署目录QML启动探针通过。
- 首次打包未发现默认FFmpeg路径，已拒绝交付不完整安装包；复用 `D:/Program Files/影资管家/ffmpeg/bin`，两个工具均验证为8.0.1，通过 `CINEVAULT_FFMPEG_BIN` 明确路径后重新打包。
- 最终载荷已校验FFmpeg、ffprobe文件与源工具SHA-256一致；部署启动探针及无业务数据库载荷检查通过。
- 安装包ProductVersion=0.1.206，210871765字节，SHA-256=`1f7dcf535cd70157c031cea01c76c2d9358cca49d5e5b4923e3ea703b9dd41cd`，旁边提供`.exe.sha256`。

## 任务目标与技术方案

中键按住并偏离锚点后，视频帧列表持续浏览，跨分页无需松开重按。保持现有分页和数据接口，仅修正滚动交互归属；通过真实鼠标事件验证内外层抢占和连续滚动。

## 文件 / 模块清单

- `src/ui/qml/components/PagedFrameList.qml`：内层中键处理器。
- `tests/qml/tst_PagedFrameList.qml`：嵌套详情滚动与持续中键输入回归。
- 本文及必要避坑记录。
- `VERSION`：遵守既有版本规则递增至0.1.206。

## 验收标准

- 在嵌套帧列表按住一次中键，可越过首批并跨240条缓存到最终帧。
- 内层滚动时外层位置不漂移；松开停止，反向拖动有效。
- 外层详情中键浏览及普通滚轮分页仍有效。
- 相关运行测试、QML门禁、构建通过。

## 已知问题

无未解决的功能问题。本轮采用独立Qt Quick运行测试验证鼠标交互；未对用户正在使用的安装版注入输入。

## 开发日志

- 2026-09-07：复现嵌套滚动目标错误；补齐帧列表中键处理器，9项QML运行检查及3项相关CTest通过，交付v0.1.206。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

读取项目规则、本文、Git状态，从“下一步”继续。不要修改用户解析数据或无关工作区内容。
