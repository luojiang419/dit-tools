# 素材浏览职责排序与PDF元数据布局优化

状态：已完成
当前阶段：3/3
最后更新：2026-09-07

## 当前状态

- 已完成：素材中心空查询使用目录查询，修改时间与资产ID排序在LIMIT前执行；两页正倒序通过独立信号同步，搜索仍按原相关度。
- 已完成：页面职责说明、统一结果栏计数、去除帧/素材/文件夹重复命中标题与计数。
- PDF已经完成自适应列、独立名称/路径、音频参数和异常条；同时修正打印原点重复边距、预览DPI及文字测量。

- 起点：`feat/architecture-reliability-20260905` / `7b5f14d`。
- 已解决的原问题：空查询按内部键/文件名排序、命中计数重复、PDF基础信息固定宽度与右侧技术列空间浪费。
- 已完成v0.1.207安装包构建、版本/大小/SHA-256校验；未安装覆盖用户当前程序。
- 用户原有 `.gitignore`、`dependencies.lock` 删除及未跟踪备份/产物保留，不纳入提交。

## 下一步

当前任务已完成。安装 `output/v0.1.207/CineVault-Setup-v0.1.207.exe` 后，在相同项目与筛选范围下对照两页普通浏览顺序；重新导出PDF即可使用新版布局，无需重新解析素材。

## 当前 TODO

- [x] 范围与排序实现、回归验证。
- [x] 重复统计去除、QML检查。
- [x] PDF布局、长名称/路径/异常/分页验证。
- [x] 构建、任务记录、提交与推送。

## 最近验证状态

- 最终7项CTest通过：MaterialCenterQueryServiceTest、LibraryWorkspacePaginationTest、MaterialCenterUiContractTest、ReportRenderEngineTest、SearchEngineTest、SearchResultFusionTest、WebSearchServiceTest，3.43秒。
- QML：34文件，723条诊断（基线724），0 error，无放宽门禁。
- 最终Release全目标构建、部署目录QML启动探针通过。
- 已实际生成3页视频元数据、10页完整PDF与对应预览；Poppler逐页视觉检查通过，名称、路径、缺失参数、异常与续页表头均可读；音频行高已按实际DPI修正。
- 测试PDF：`output/pdf/material-redesign/video-metadata.pdf`；完整回归PDF：同目录`full-report.pdf`。这些是测试数据示例，并非用户完整项目的实际导出。
- 安装包：210847837字节，ProductVersion=0.1.207，SHA-256=`44569ed61c4505b3b47d99f1c8b42c17fb7f6f1e43a015153d3db110a86ebbeb`，旁边有`.exe.sha256`。
- 打包脚本已打入FFmpeg并自动清理暂存目录；启动探针在清理前通过。清理后的追加载荷检查未执行成功，不将其视为已通过。
- 素材浏览模块提交：`94ff595`，已推送。PDF与交付提交见当前分支最终Git历史。

## 关键修改与开发日志

- 2026-09-07：普通浏览直接查询目录并在LIMIT前排序；跨页相关度逻辑未改；素材中心与素材库通过独立排序信号联动。
- 2026-09-07：报表采用五列比例（序号、预览、文件信息、画面参数、时长与音频），动态行高、独立异常条，修正打印原点与DPI。
- 2026-09-07：Git的schannel握手失败；相同代理下使用OpenSSL后端验证连接并成功推送，未关闭证书验证。经验已写入避坑指南。

## 任务目标与技术方案

- 素材库：当前项目文件浏览、预览、收藏、技术检查；素材中心：跨项目检索、筛选、解析和内容复用。保留现有页面与功能，通过范围说明消除混淆。
- 无关键词时按修改时间排序，同时间用项目内资产ID保证稳定；两页正倒序联动。关键词查询按相关度，不改变检索评分。
- 搜索区仅一个命中汇总；混合类型仍保留必要分组名称，不再重复计数。
- PDF沿用 Qt 原生渲染、现有配色和字体，按页面宽度分配列宽，文件名与路径独立排版，参数拆分横向利用空间。

## 文件 / 模块清单

- application/MaterialCenterQueryService、domain/SearchTypes：普通浏览查询。
- ui/viewmodels/LibraryWorkspaceViewModel、MaterialCenterViewModel、app/AppContext：排序状态与联动。
- ui/qml/workspaces/LibraryWorkspace.qml、MaterialCenterWorkspace.qml：页面职责与结果栏。
- core/report/ReportRenderEngine.cpp：PDF自适应布局。
- tests/unit、CMakeLists.txt：必要的查询和报表回归。

## 验收标准

- 同一项目、同一素材类型及范围下两处普通浏览顺序一致，正倒序均有效，搜索相关度不退化。
- 单类搜索结果不连续显示多个命中数，空查询显示素材浏览语义。
- PDF横向空间被合理使用，长名称、路径、缺失参数、音频流、异常与跨页表头可读。
- 相关测试、QML检查、Release构建通过；真实PDF渲染检查完成。

## 已知问题

无未解决的本次功能问题。素材中心既有最多2000条结果上限保留，并已明确提示筛选缩小范围；普通浏览正倒序在会话内联动，重启后两页统一恢复默认倒序。未对用户正在使用的安装版注入操作，运行验收使用查询测试、离屏PDF渲染及部署目录启动探针。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

阅读项目规则及本文，检查Git与当前源代码，从“下一步”继续，不重复已完成阶段。
