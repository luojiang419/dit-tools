# 显式源码版本发布规则修复 - 代码前后对比

## 背景

当前状态：

- GitHub Latest Release：`v0.1.180`
- 本机已安装版本：`v0.1.185`
- 仓库 `VERSION`：`0.1.186`

旧发布脚本只允许：

- `VERSION == Latest`：要求 CI 生成版本号提交；
- `VERSION == Latest + 1`：发布下一补丁版本。

如果继续按旧规则，只能发布 `v0.1.181`，这会低于用户本机 `v0.1.185`，客户端不会检测到更新。

## 修改文件

- `tool/resolve_release_version.ps1`
- `tool/test_release_version.ps1`

## 修复前

```powershell
$versionTag = Get-NextCineVaultReleaseVersion `
    -LatestVersion $LatestVersion `
    -MinimumVersion $MinimumVersion

if ($comparisonWithLatest -eq 0) {
    Write-WorkflowOutput -Name 'version_tag' -Value $versionTag
    return
}
if ($sourceVersionTag -cne $versionTag) {
    throw "Repository VERSION $sourceVersionTag must equal latest release $normalizedLatestVersion or the next release $versionTag."
}
```

问题：`VERSION=0.1.186`、Latest=`v0.1.180` 时，脚本强制失败。

## 修复后

```powershell
$nextVersionTag = Get-NextCineVaultReleaseVersion `
    -LatestVersion $LatestVersion `
    -MinimumVersion $MinimumVersion

if ($comparisonWithLatest -eq 0) {
    Write-WorkflowOutput -Name 'version_tag' -Value $nextVersionTag
    return
}
if ($comparisonWithLatest -lt 0) {
    throw "Repository VERSION $sourceVersionTag is lower than latest release $normalizedLatestVersion."
}

$comparisonWithNext = Compare-CineVaultVersions -Left $sourceVersionTag -Right $nextVersionTag
if ($comparisonWithNext -gt 0) {
    Write-Host "Repository VERSION $sourceVersionTag is higher than the next patch $nextVersionTag; publishing the explicit source version."
}

Write-WorkflowOutput -Name 'should_publish' -Value 'true'
Write-WorkflowOutput -Name 'version_tag' -Value $sourceVersionTag
```

新规则：

- 源码版本等于 Latest：不重复发布，要求版本提交；
- 源码版本低于 Latest：拒绝；
- 源码版本高于 Latest：发布源码显式版本；
- 源码版本高于 `Latest + 1` 时打印说明，避免静默跳号。

## 新增测试

`tool/test_release_version.ps1` 新增脚本级测试：

- `VERSION=0.1.186`、Latest=`v0.1.180`：`should_publish=true`，`version_tag=v0.1.186`
- `VERSION=0.1.180`、Latest=`v0.1.180`：`should_publish=false`，`version_bump_required=true`，建议 `v0.1.181`
- `VERSION=0.1.179`、Latest=`v0.1.180`：拒绝发布

## 本地打包结果

- 安装包：`G:\data\app\DIT-tools\output\v0.1.186\CineVault-Setup-v0.1.186.exe`
- 大小：`210699786` bytes
- SHA-256：`d48f5847d9375ca9ac0bebbd26adaba4039f1e4670180b7783cc74afce3adfd3`
- 签名状态：`NotSigned`

## 注意

本地没有执行 `tool/test_installer_startup.ps1`，因为该脚本会写临时卸载注册表，脚本自身要求仅在 disposable CI runner 上使用。当前本地已完成构建脚本内的 staging 启动探针；完整发布仍需 GitHub Actions 干净 runner 复核。
